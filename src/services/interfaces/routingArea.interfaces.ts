import { IRootState } from "./root.interfaces";

export interface IRoutingAreaState {
  stateGetListRoutingAreas: IRootState;
  stateCreateOneRoutingArea: IRootState;
  stateUpdateOneRoutingArea: IRootState;
}
